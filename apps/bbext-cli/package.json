{
  "name": "@bbext/cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "bbext": "dist/bbext"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun run build:compile && bun run build:runtime-sharp",
    "build:compile": "bun build src/cli.ts --minify --target bun --outfile dist/bbext --compile --compile-autoload-package-json --external sharp",
    "build:runtime-sharp": "bun run scripts/embed-sharp-runtime.js"
  },
  "dependencies": {
    "@bbext/lib": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}